#ifndef ATTRIBUTE_H
#define ATTRIBUTE_H

#include "types.h"

typedef enum Attribute_en
{
    ATTR_STRENGTH,
    ATTR_DEXTERITY,
    ATTR_RESILENCE,
    ATTR_AGILITY,
    ATTR_COUNT
} Attribute;

#endif