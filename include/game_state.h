#ifndef GAME_STATE_H
#define GAME_STATE_H

#include "state.h"

typedef enum GameStateIdentifier_en
{
    GAME_STATE_TITLE,
    GAME_STATE_STORY,
    GAME_STATE_COMBAT,
    GAME_STATE_KEYBOARD
} GameStateIdentifier;

#endif